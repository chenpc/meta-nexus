# Development mode: use local source trees instead of fetching from GitHub
# Activated by ./build.sh --dev

INHERIT += "externalsrc"

EXTERNALSRC:pn-storage-daemon = "${TOPDIR}/../../storage-daemon"
EXTERNALSRC_BUILD:pn-storage-daemon = "${TOPDIR}/../../storage-daemon"

EXTERNALSRC:pn-cli-shell = "${TOPDIR}/../../cli-shell"
EXTERNALSRC_BUILD:pn-cli-shell = "${TOPDIR}/../../cli-shell"

EXTERNALSRC:pn-libnexus = "${TOPDIR}/../../libnexus"
EXTERNALSRC_BUILD:pn-libnexus = "${TOPDIR}/../../libnexus"
